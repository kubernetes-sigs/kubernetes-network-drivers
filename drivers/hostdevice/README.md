# Hostdevice Network Driver

This is a simple example of a network driver for Kubernetes that exposes the network devices via DRA and allow Pods to use them.

## Installation

```bash
make image-build image-push
```

## Usage  



